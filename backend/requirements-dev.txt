# Dev / test extras — not installed in production
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # async test client for FastAPI
factory-boy>=3.3
faker>=24.0
anyio[trio]>=4.0

# Code quality
ruff>=0.4
mypy>=1.10
pre-commit>=3.7

# Type stubs
types-passlib
types-boto3
